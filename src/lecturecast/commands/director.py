"""Director commands are introduced in M1 Loop 3.

Keeping this module explicit prevents agents from mistaking conversation history
for a Director session before the server-backed state machine exists.
"""

